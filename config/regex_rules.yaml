rules:
  forex:
    - '^[A-Z]{6}$'        # EURUSD style
    - '^[A-Z]{3}/[A-Z]{3}$' # EUR/USD style
  crypto:
    - '.*(USDT|BTC|ETH|BUSD)$' # common exchange suffixes
  funds:
    - '^TRFUND'           # internal fund identifiers
  equities:
    - '^[A-Z]{1,5}$'      # short tickers
